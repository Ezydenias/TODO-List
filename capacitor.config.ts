import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.todoroo.app',
  appName: 'todo-roo',
  webDir: 'dist/TODO-ROO',
  bundledWebRuntime: false
};

export default config;
